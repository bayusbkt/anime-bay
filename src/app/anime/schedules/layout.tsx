import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'AnimeBay - Schedules',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
